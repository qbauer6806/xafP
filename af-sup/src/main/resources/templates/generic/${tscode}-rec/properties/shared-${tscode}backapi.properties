{{ ansible_managed | comment }}

${TSCODE}-CONN=${TSCODE}
${TSCODE}-CLASS=org.postgresql.Driver
${TSCODE}-URL=jdbc:postgresql://gents-eosd1-3.monaco-gouvernement.mc:5432/${tscode}
${TSCODE}-USER=${tscode}
${TSCODE}-PASS=${TSPGPWD}

# Container utilisé pour l'appel à FILE
mc.gouv.${tscode}.backserver.file.containerId=ROOT

mc.gouv.${tscode}.backserver.processDefinitionKey=process


#createJWTToken("<mail-api.jwt.secret>", "${TSCODE}", new String[] { "USER"}, "MAIL", null,null);
mc.gouv.${tscode}.backserver.mail.jwt=${TSMAILJWT}
#String jwt = createJWTToken("<file-api.jwt.secret>", "${TSCODE}", new String[] { "USER"}, "FILE", null,null);
mc.gouv.${tscode}.backserver.file.jwt=${TSFILEJWT}
mc.gouv.${tscode}.backserver.usagerscache.duration=60000

#${tscode}.override.spring.jpa.properties.hibernate.default_schema=${TSCODE}

##NORMALEMENT CES PROPRIETES DEVRAIENT ETRE DANS ${tscode}-backserver.properties
##Le problème est que GouvPropertiesResolver est partagé aujourd'hui entre l'API et le Back
# URL de redirection vers le front pour la création des demandes en tant qu'usager courrier
mc.gouv.${tscode}.backserver.front.url=https://front-rec.monaco-gouvernement.mc/${TSCONTEXTE}/
mc.gouv.${tscode}.backserver.front.formstartpage=etape_usager.html

# URL du back-office
#A VOIR SUIVANT LE DEPLOIEMENT DE LA SECURISATION LOGON SI HTTPS
mc.gouv.${tscode}.backserver.back.url=https://${tscode}-back-rec.monaco-gouvernement.mc/${tscode}/

# Page d'aide
mc.gouv.${tscode}.backserver.help.url=http://intra
# Clé partagée avec le front pour le calcul du sig
mc.gouv.${tscode}.backserver.front.key=${TSFRONTKEY}

mc.gouv.${tscode}.jms.topic=demandes


mc.gouv.${tscode}.elasticsearch.reindex.bulksize=100
mc.gouv.${tscode}.jms.data.dir=/www/local/data/artemis
mc.gouv.${tscode}.jms.redelivery.delay=2000
mc.gouv.${tscode}.jms.redelivery.multiplier=2
mc.gouv.${tscode}.jms.redelivery.maxAttemps=100
mc.gouv.${tscode}.jms.topic.consumer.user=${tscode}-user
mc.gouv.${tscode}.jms.topic.consumer.password=${tscode}-pass
mc.gouv.${tscode}.jms.dlq=dlq

##############################################################################


${tscode}.override.spring.jpa.properties.hibernate.default_schema=${TSCODE}

mc.gouv.${tscode}.jms.topic.sender.user=${tscode}-user
mc.gouv.${tscode}.jms.topic.sender.password=${tscode}-pass
mc.gouv.${tscode}.jms.topic.subscription.key=${tscode}-subscription


mc.gouv.${tscode}.elasticsearch.clustername={{elasticsearch_cluster_name}}
mc.gouv.${tscode}.elasticsearch.host=${tscode}-eose1-3
mc.gouv.${tscode}.elasticsearch.port=9300
mc.gouv.${tscode}.elasticsearch.nodename={{inventory_hostname}}

##############################################################################
# Properties commune GENTS
##############################################################################

#Utiliser par af pour sauvegarder tous les appels à file
mc.gouv.af.back.file.url=http://as-api-1-rec.monaco-gouvernement.mc:30490/file/api/v1/file
#Utiliser par DEM pour mettre à jour les metadata
mc.gouv.demarches.external.filews.file.url=http://as-api-1-rec.monaco-gouvernement.mc:30490/file/api/v1/file

mc.gouv.af.back.mail.url=http://as-api-1-rec.monaco-gouvernement.mc:30480/mail/api/v1/mail


# Properties de DEM
mc.gouv.demarches.external.usagers.url=http://as-api-1-rec.monaco-gouvernement.mc:20110/servicerest/rest/usagers
mc.gouv.demarches.external.pays.url=http://as-api-1-rec.monaco-gouvernement.mc:20110/servicerest/rest/pays
mc.gouv.demarches.containerId=ROOT

#url du support
mc.gouv.af.back.contactSupport.url=http://sos/glpi/
