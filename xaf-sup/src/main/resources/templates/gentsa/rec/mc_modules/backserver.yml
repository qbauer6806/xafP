---
appli: ${tscode}
type: backserver
name: ${tscode}-backserver
maven:
    group_id: mc.gouv.${tscode}
    artifact_id: ${tscode}-backserver
context: /${tscode}
url: https://${tscode}-back-rec.monaco-gouvernement.mc/${tscode}
http_port: 10${tsport}0
jmx_port: 10${tsport}1
rmi_port: 10${tsport}2
xms: ${xms}
xmx: ${xmx}
tags:
    - ${tscode}backapi
java_extra_opts: -Dspring.profiles.active=gouv -Dfile.encoding=UTF-8 
